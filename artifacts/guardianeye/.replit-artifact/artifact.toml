kind = "web"
previewPath = "/"
title = "GuardianEye"
version = "1.0.0"
id = "artifacts/guardianeye"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20287

[services.development]
run = "pnpm --filter @workspace/guardianeye run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/guardianeye", "run", "build" ]
publicDir = "artifacts/guardianeye/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20287"
BASE_PATH = "/"
